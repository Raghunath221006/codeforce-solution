#include <bits/stdc++.h>
using namespace std;

int main() {
	string a;
	string b;
	cin>>a;
	vector<char>temp;
	for(auto x:a){
	    if(x!='+'){
	        temp.push_back(x);
	    }
	}
	sort(temp.begin(),temp.end());
	for(auto x:temp){
	    b+=x;
	    b+='+';
	}
	b.pop_back();
	cout<<b;
	

}
